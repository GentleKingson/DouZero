extends: _defaults.yaml.inc
metadata: {name: legacy_a1_legacy, description: Legacy A1 formal control under legacy rules}
variant: legacy_a1
model:
  family: legacy_a1
  version: legacy-a1
  config:
    __replace__: {architecture: legacy_lstm, hidden_size: 512, observation: legacy}
features: {role_model: false}
runtime:
  profile: legacy_a1_production_v1
  topology: single_process
  num_actors: 12
  games_per_actor: 1
  policy_lag_limit: 0
initialization: {source: legacy-a1-seeded-fresh-v1}
